{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Debug",
            "type": "shell",
            "command": "rm -f *;rm -f -r *;cmake -DCMAKE_BUILD_TYPE=Debug ..;cmake --build .",
            "options": {
                "cwd": "build"
            },
            "problemMatcher": [],
            "group": "build"
        }
    ]
}