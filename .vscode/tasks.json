{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Release",
            "type": "shell",
            "command": "rm -f *;rm -f -r *;cmake -DCMAKE_BUILD_TYPE=Release ..;cmake --build .",
            "options": {"cwd": "build"},
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}